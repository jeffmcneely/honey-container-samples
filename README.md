# honey-container-samples

Container samples for testing and demonstration.

## aws-demo
Visualizes AWS VPC resources (VPCs, EC2 instances, interfaces, gateways) as a tree structure using the Rich library.

## workload-demo
Simulates CPU and memory workloads with configurable intensity and duration via environment variables.
